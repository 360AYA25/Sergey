Я вижу проблему! Похоже, что меня вызвали как **обычного Claude Code агента**, но передали JSON в формате, который используется для специализированных n8n-агентов (n8n-planner-gpt, n8n-planner-gemini и т.д.).

**Ситуация:**
- Я — это **основной Claude Code**, а не специализированный n8n-агент
- Специализированные агенты вызываются через `agents/` директорию и используют API-режим
- Они должны возвращать **только JSON**, без объяснений

**Что нужно сделать:**

Если вы хотите создать webhook для отправки сообщения в Telegram, у нас есть 2 варианта:

1. **Использовать orchestrator.js** (рекомендуется):
   ```bash
   node orchestrator.js "Create webhook that sends message to Telegram"
   ```
   
2. **Я помогу напрямую** (если orchestrator не нужен):
   - Создам workflow n8n вручную
   - Использую Webhook + Telegram node
   - Сделаю это простым и понятным для новичка

Что выбираем?
