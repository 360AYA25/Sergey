Готово! Создан простой webhook воркфлоу:

**Структура:**
- `Webhook` (POST /simple-webhook) → `Format Response` (Set node)

**URL для тестирования:**
```
https://n8n.sergey.app/webhook-test/simple-webhook
```

**Продакшн URL:**
```
https://n8n.sergey.app/webhook/simple-webhook
```

**Что возвращает:**
```json
{
  "status": "success",
  "message": "Data received successfully", 
  "received_data": {...полученные данные...},
  "timestamp": "2025-10-25T14:30:00.000Z"
}
```

**Рекомендации валидатора:**
1. Добавить аутентификацию (API key) для продакшна
2. Проверить корректность всех полей в ответе

Воркфлоу максимально простой (complexity: 1) - идеален для новичков! 🎯
